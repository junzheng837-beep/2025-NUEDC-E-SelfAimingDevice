# 队员代码提交流程指南

大家好，为了保证咱们电赛代码库的安全和整洁，避免互相覆盖代码，请大家在编写和上传代码时，严格按照以下 6 个步骤操作：

## 第一步：同步云端最新代码（每次写代码前必做）
在开始今天的工作之前，必须先保证你本地的代码是最新的。打开终端（Git Bash），进入工程目录。

请**分两次**复制并执行以下两条命令：

`git checkout main`

`git pull origin main`

> **注**：这能确保你拿到队长或其他队员刚刚合并到主分支的最新功能。

---

## 第二步：创建自己的独立分支
**严禁直接在 main 分支上写代码和提交！**
你要开发什么功能，就建一个什么名字的分支。例如你要写电机 PID 算法，就执行：

`git checkout -b feature/motor_pid`

> **注**：如果是修复 bug，可以命名为 `fix/uart_error`。执行完这句后，你就已经自动切换到新分支了。

---

## 第三步：在 CCS 中编写代码并保存
正常使用 Code Composer Studio 编写代码。
* **注意**：如果需要修改引脚，请统一在 `empty.syscfg` 界面中修改并保存。严禁在代码中直接写死寄存器配置。

---

## 第四步：暂存并提交代码
当你完成了一个小功能，确认编译通过后，在终端**依次**执行以下两条命令将代码提交到你的本地分支：

`git add .`

`git commit -m "feat: 完成了电机 PID 基础驱动编写"`

> **注**：提交信息请遵循规范（`feat:` 代表新功能，`fix:` 代表修Bug，`docs:` 代表改文档）。由于队长已经配置好了 `.gitignore`，`add .` 不会将编译产生的垃圾文件加进去，放心执行。

---

## 第五步：将你的分支推送到云端
把你写好的独立分支推送到 GitHub 仓库（请把最后的 `你的分支名` 替换为你第二步创建的实际名称）：

`git push -u origin 你的分支名`

> **例如**：`git push -u origin feature/motor_pid`

---

## 第六步：在 GitHub 提交 PR (Pull Request)
1. 登录 GitHub 网页端，进入咱们的仓库主页。
2. 页面顶部会自动弹出一个黄色的提示框，显示你的分支刚刚有推送，点击绿色的 **Compare & pull request** 按钮。
3. 在说明框里简单写一下你这次提交实现了什么功能，或者还有什么已知问题。
4. 点击 **Create pull request**。

完成以上步骤后，你的任务就结束了。队长会收到通知并进行 Code Review（代码审查）。审查通过后，队长会把你的代码合并到 `main` 分支。
