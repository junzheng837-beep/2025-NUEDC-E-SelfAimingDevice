# 智能车循迹 PID 与底盘物理参数调校档案

> **最后更新时间**：2026-06-16
> **供电系统**：12V 供电 (例如 3S 满电)
> **电机硬件**：MG310 (额定标称 7.4V/400RPM，13线霍尔，1:20减速箱)

---

## 1. 完美循迹参数组合 (黄金版)

经过实车大量反复测试与评分，以下组合达到了极高的稳定性（转圈无画龙，车身姿态顺滑）：

*   **基础巡航速度 (Basic_Speed / Target_Speed)**：`45.0`
    *   *注：软件设定值 45 对应的真实物理线速度约为 0.72 米/秒。*

*   **转向环 PID (pid_Turn)**
    *   **Kp (比例)** = `5.0` 
        *   *(决定了对黑线偏移的响应力度，5.0 提供了恰到好处的牵引力，过大易画龙)*
    *   **Ki (积分)** = `0.02`
        *   *(消除长时间的稳态静态误差，微弱的 0.02 足以让车头在长直道上自动贴合中心，但需防范出弯时积分残留导致轻微偏左)*
    *   **Kd (微分)** = `145.0`
        *   *(提供了强大的阻尼力，145.0 是消除车身抖动、防止画龙的核心防线)*

*   **速度环 PID (底层电机控制)**
    *   **Kp** = `25.0`
    *   **Ki** = `3.0`
    *   **Kd** = `0.04`
    *   **核心修改点**：`Ki_Limit_MAX` 必须设置为 `9000`（或者更高），否则会被限幅在 3000，导致电机满油门只能发挥出 48% 的性能。

---

## 2. 底盘物理极限摸底报告

经过悬空全油门（`SP=400`）暴力压榨测试，得到以下真实物理反馈：

### 2.1 物理速度极值 (悬空软件值)
*   **左轮 (M1) 极限**：`132.3`
*   **右轮 (M2) 极限**：`149.5`
*   *现象总结*：右轮动力比左轮强劲约 10%。在进行高速过弯（尤其是向左大角度转弯）时，由于左轮本身较弱且极限较低，容易出现向内侧分配动力不足的问题。

### 2.2 硬件逻辑与软件代码的换算秘密
底层的物理脉冲已经通过纯手工转动 1 圈精确测量：
*   **实际物理转 1 圈产生的底层脉冲数 = `260` 左右**。
    *   推导：13线(单相) × 20减速比 = 260脉冲。证明底层定时器使用的是单相或 1倍频 捕获逻辑。

*   **代码计算缩放误差**：
    由于 `Encoder.h` 中的数学公式：
    1. 使用了 `BEIPIN=8`（实际上应该是 1）
    2. 车轮周长算成了 `PI * RR`（漏乘了 2）
    导致代码算出的**软件速度值，刚好是真实物理速度的 `1/16`**。

*   **结论**：不要去改代码底层的测速公式！直接把这种“缩放过的软件速度值”当成标准单位即可。软件里下发 `45`，对车子而言就已经非常快（约 0.72m/s）；软件下发 `140` 时，12V供电下的电机实际上处于超频发狂状态（物理转速突破 900+ RPM，线速度超过 2.2m/s）。

---

## 3. 下一步优化建议与避坑指南

1. **避免直道速度设置过高**
   * 直道最高速度**切勿超过 `110`**。
   * **原因**：因为右轮的绝对物理极限是 149。如果设定速度超过 110，当需要差速转向时（比如外侧轮需要加速到 110+40=150），外侧轮的 PWM 已经 100% 满载，物理上无法再加速，此时车子会瞬间丧失转向力（推头冲出赛道）。

2. **解决出弯偏左的隐患**
   * 当前在 `45` 速度下，出弯后会偶尔“偏左直行”。
   * **修复方向**：建议在判断出“已经脱离弯道进入直道”的瞬间，在代码中强行给转弯积分项清零：`pid_Turn.KiOut = 0;`，防止累计的积分力量在直道上持续推拽车头。

3. **动态变速预留方案**
   * 如未来需要刷极速得分，建议采用：**直道设定 `SP=85`，弯道判定后瞬间强制改回 `SP=45`** 的双模策略。

---

## 4. 各挡位速度与完美 PID 参数速查表

在不同的行驶速度下，车身的惯性和传感器的前瞻反应时间不同，因此转向环的 PID 必须随速度动态切换。以下为经过实车验证或推算的几个典型挡位参数：

### 🏁 挡位一：低速稳定档 (Basic_Speed = 30.0)
*适用场景：初阶测试、极窄发卡弯、极其复杂的迷宫阵*
*   **物理线速度**：约 0.48 m/s
*   **转向 PID**：
    *   `Kp = 4.0`
    *   `Ki = 0.01`
    *   `Kd = 120.0`
*   **表现**：车身极度平稳，不画龙，但过急弯时可能会显得“不够灵敏”，因为速度慢，积分和微分的作用不需要太强。

### 🏁 挡位二：中速竞赛档 (Basic_Speed = 45.0)  【当前黄金组合】
*适用场景：常规电赛寻线、环岛、大圆弧弯*
*   **物理线速度**：约 0.72 m/s
*   **转向 PID**：
    *   `Kp = 5.0` 
    *   `Ki = 0.02` (最优解，能消除静差且无明显画龙)
    *   `Kd = 145.0`
*   **表现**：响应迅猛，车头紧贴黑线，过弯姿态顺滑。
