# 智能车循迹 PID 与底盘物理参数调校档案

> **最后更新时间**：2026-06-16
> **供电系统**：12V 供电 (例如 3S 满电)
> **电机硬件**：MG310 (额定标称 7.4V/400RPM，13线霍尔，1:20减速箱)

---

## 1. 【重载前探版】循迹参数组合 (最新调校)

*适用状态：车盘增加重物配重 + 灰度传感器物理前伸 + 纯物理PID硬解（无刹车/无直道清零代码）。由于惯性激增，这是多次魔鬼拉扯后得出的唯一完美解。*

*   **基础巡航速度 (Basic_Speed / Target_Speed)**：`35.0`
    *   *注：物理线速度约 0.56 米/秒。在纯硬解前提下，此为重载车防脱线的天花板速度。*

*   **转向环 PID (外环)**
    *   **Kp (比例)** = `3.0` (降低敏感度，彻底消除直道轻微抖动)
    *   **Ki (积分)** = `0.0` (零积分：彻底抛弃积分修正，纯靠PD硬抗)
    *   **Kd (微分)** = `100.0` (恰到好处的阻尼，抗离心力且不放大噪声)

*   **速度环 PID (内环)**
    *   **Kp** = `35.0` (超高刚性，强行对抗重载带来的惯性迟滞)
    *   **Ki** = `5.0`
    *   **Kd** = `0.0`
    *   **核心保障**：`Ki_Limit_MAX` 恢复为安全阈值 `500`，防范异常风卷。

---

## 2. 【轻载原版】循迹参数组合 (历史黄金版)

*适用状态：未加配重的初始原车轻载状态。*

*   **基础巡航速度 (Basic_Speed / Target_Speed)**：`45.0`
    *   *注：物理线速度约 0.72 米/秒。*

*   **转向环 PID (外环)**
    *   **Kp** = `5.0` 
    *   **Ki** = `0.02`
    *   **Kd** = `145.0`

*   **速度环 PID (内环)**
    *   **Kp** = `25.0`, **Ki** = `3.0`, **Kd** = `0.04`

---

## 3. 底盘物理极限摸底报告

经过悬空全油门（`SP=400`）暴力压榨测试，得到以下真实物理反馈：

### 3.1 物理速度极值 (悬空软件值)
*   **左轮 (M1) 极限**：`132.3`
*   **右轮 (M2) 极限**：`149.5`
*   *现象总结*：右轮动力比左轮强劲约 10%。在进行高速过弯（尤其是向左大角度转弯）时，由于左轮本身较弱且极限较低，容易出现向内侧分配动力不足的问题。

### 3.2 硬件逻辑与软件代码的换算秘密
底层的物理脉冲已经通过纯手工转动 1 圈精确测量：
*   **实际物理转 1 圈产生的底层脉冲数 = `260` 左右**。
    *   推导：13线(单相) × 20减速比 = 260脉冲。证明底层定时器使用的是单相或 1倍频 捕获逻辑。

*   **代码计算缩放误差**：
    由于 `Encoder.h` 中的数学公式：
    1. 使用了 `BEIPIN=8`（实际上应该是 1）
    2. 车轮周长算成了 `PI * RR`（漏乘了 2）
    导致代码算出的**软件速度值，刚好是真实物理速度的 `1/16`**。

*   **结论**：不要去改代码底层的测速公式！直接把这种“缩放过的软件速度值”当成标准单位即可。软件里下发 `45`，对车子而言就已经非常快（约 0.72m/s）；软件下发 `140` 时，12V供电下的电机实际上处于超频发狂状态（物理转速突破 900+ RPM，线速度超过 2.2m/s）。

---

## 4. 下一步优化建议与避坑指南

1. **避免直道速度设置过高**
   * 直道最高速度**切勿超过 `110`**。
   * **原因**：因为右轮的绝对物理极限是 149。如果设定速度超过 110，当需要差速转向时（比如外侧轮需要加速到 110+40=150），外侧轮的 PWM 已经 100% 满载，物理上无法再加速，此时车子会瞬间丧失转向力（推头冲出赛道）。

2. **解决出弯偏左的隐患 (如需提速)**
   * 如果未来还需要在重载状态下突破 35 极速，唯一途径是打破“纯硬解”规则。
   * **修复方向**：建议在判断出“已经脱离弯道进入直道”的瞬间，在代码中强行给转弯积分项清零：`pid_Turn.KiOut = 0;`，防止累计的积分力量在直道上持续推拽车头。

3. **动态变速预留方案**
   * 如未来需要刷极速得分，建议采用：**直道设定 `SP=85`，弯道判定后瞬间强制改回极速安全阈值 (`SP=35` 或 `SP=45`)** 的双模策略。
