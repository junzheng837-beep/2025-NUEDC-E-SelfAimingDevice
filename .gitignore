# ==========================================
# TI Code Composer Studio / Eclipse 专用忽略清单
# ==========================================

# 1. 编译生成的文件夹 (必须忽略！否则每点一次编译都会产生几十个冲突)
Debug/
Release/

# 2. 编译生成的中间文件和目标文件
*.out
*.obj
*.map
*.hex
*.bin
*.asm
*.d

# 3. Eclipse / CCS 本地工作空间缓存和个人日志
.metadata/
.settings/
.launches/
*.local
*.log

# 4. 操作系统产生的垃圾文件
.DS_Store
Thumbs.db

# ==========================================
# ⚠️ 以下是必须保留在 GitHub 上的关键工程文件 ⚠️
# （不需要写进代码里，仅作为核对清单）
# - .project        (Eclipse 工程描述)
# - .cproject       (C/C++ 构建配置)
# - .ccsproject     (CCS 专属配置)
# - *.ccxml         (目标仿真器配置文件，没它队友无法烧录)
# - *.syscfg        (TI SysConfig 配置文件，若使用了该工具)
# ==========================================
