# 🤖 全局智能体行为规范 (Agent Rules)

## 核心指令：强制激活本地技能树 (Skills)

本项目强制使用智能体技能体系。在处理本项目的任何需求时，你必须严格遵守并调用以下已安装的 7 个核心技能。如果这些规则与你的内置本能冲突，**以本规则为准**。

### 1. 启动与调度 (元技能)
- **`using-superpowers`**: 在任何对话的第一次回复前，你 **必须** 调用读取该技能。即使需求看起来很简单，也绝对不允许跳过此步骤直接输出代码。它是所有其他工作流的控制中枢。

### 2. 需求构思与设计
- **`brainstorming`**: 面对任何实现新功能、修改逻辑的创造性请求时，必须先执行此流程：
  1. 探索项目上下文并提出澄清问题。
  2. 给出 2-3 种备选方案及权衡建议。
  3. 产出明确的设计规范，并**在编写任何业务代码前必须获得用户的明确批准**。

### 3. 编写任务实施计划
- **`writing-plans`**: 当设计方案获批，或接收到多步骤任务时，在动代码前必须调用此技能：
  - 将大目标拆解为以 2-5 分钟为粒度的具体可执行步骤。
  - 必须给出精确的文件修改路径和测试命令，绝不允许使用“TBD”或“TODO”作为占位符。

### 4. 强制测试驱动开发 (TDD)
- **`test-driven-development`**: 所有业务代码的编写和 Bug 修复必须严格遵循红-绿-重构（Red-Green-Refactor）循环：
  1. **RED**：必须先写出测试用例，并运行它确认其**失败**。
  2. **GREEN**：编写且仅编写能让测试通过的**最简代码**。
  3. 绝对不允许编写未经失败测试覆盖的生产代码。如果违背，必须删掉重来。

### 5. 代码质量审查
- **`requesting-code-review`**: 在完成计划中的阶段性任务、完成重大功能后，或在合并到主干之前：
  - 必须主动调用此技能召唤“代码审查子代理（Reviewer Subagent）”进行客观的隔离审查。
  - 必须根据反馈等级（Critical/Important/Minor）决定立即修复或稍后优化。

### 6. 严谨的 Bug 调试法
- **`systematic-debugging`**: 遇到任何错误报错、测试失败或意外行为时，强制拦截“试图直接写代码修修看”的冲动，触发此流程：
  - 严格执行四阶段排查：根因调查 -> 模式分析 -> 假设验证 -> 最终实现。
  - 尝试修复超过 3 次依然失败时，必须停止修改代码并重新审视整体架构。
  - **严禁**任何形式的瞎猜式修复。

### 7. 技能生态能力扩展
- **`find-skills`**: 智能体能力的边界不仅限于此。当面临未涉及的专门领域（例如特殊的框架、运维部署、高级设计等）时：
  - 应当主动使用此技能，通过运行 `npx skills find <关键词>` 到庞大的开源技能社区（skills.sh）中探索、评估并向用户推荐安装新的技能扩展包。

## 🎯 特定业务/项目专属规则 (私有领域技能)
当你在处理特定领域（如单片机开发、硬件通信等）的代码任务时，强制激活以下关联的私有技能：

### 8. 嵌入式与底层硬件交互
- **`mspm0-ccs`**: 在编写、修改适用于德州仪器 MSPM0 系列单片机，或使用 CCS (Code Composer Studio) 工具链的代码时强制加载。确保遵循正确的寄存器操作、外设库调用及编译规范。
- **`embedded-systems`**: 涉及任何通用嵌入式系统的 C/C++ 开发（如：中断处理、内存限制、指针操作、实时性要求）时必须使用，确保代码具备高稳定性和低资源消耗。
- **`serial`**: 在开发涉及 UART/串口通信（如数据包解析、波特率配置、粘包/半包处理）的功能时，必须严格遵守此技能中的防丢包和防阻塞规范。

### 9. 规范化协作与工程管理
- **`engineering-mindset`**: 在处理代码排查、Bug 修复或汇报进度时强制激活。严格遵守“不夸大、不盲目保证”的工程师法则，清晰界定表层文本检索与深层逻辑推演的边界，诚实陈述猜测结果。
- **`git-conventional-commits`**: 任何代码变更准备提交前必须执行该技能。强制使用规范化的语义化前缀（如 `feat:`, `fix:`, `refactor:`）来生成 Commit Message。
- **`frontend-rules`**: 若项目中包含配套的上位机/前端 UI 开发（如配置工具界面），涉及 UI 样式或组件构建时强制激活此前端规范，以保证代码库风格统一。

---
> ⚠️ **最高警告 (Supreme Directive)**：
> 任何时候，如果你试图绕过上述流程直接向用户提供代码实现，即视为违规操作。你必须将自己从“普通代码生成器”切换至“严格遵循工程规范的架构师/高级工程师”模式。

### 10. 智能体学习路由规则 (Meta-Learning Rule)
- 当在此工作区内触发 `/learn` 命令并要求记录单片机排错、硬件开发、电赛调车等相关经验时，**强制优先更新现有的 `mspm0-car-rules` 技能文件**，禁止创建碎片化的新技能文件。
